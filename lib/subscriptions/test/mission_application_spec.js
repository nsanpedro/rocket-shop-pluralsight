var assert = require("assert");
var MembershipApplication = require('../lib/membership_application');

describe("Applying for a mission", function(){
	describe("Using valid email, first, last, height, age, weight", function(){
		it("is valid");
		it("it reports a valid email");
		it("it reports a valid height");
		it("it reports a valid age");
		it("it reports a valid weight");
		it("it reports a valid name");
	});
});